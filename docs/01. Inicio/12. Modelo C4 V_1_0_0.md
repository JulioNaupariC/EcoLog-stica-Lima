# Arquitectura de software — Modelo C4

| Metadato | Valor |
|---|---|
| Proyecto | EcoLogística Lima |
| Versión | 1.0.0 |
| Fecha | 04/09/2026 |

## Decisiones y atributos arquitectónicos

La arquitectura separa experiencia web, API de dominio, procesamiento de optimización y almacenamiento para soportar rendimiento, disponibilidad, seguridad y evolución de las reglas. Las integraciones externas se consumen mediante adaptadores; una indisponibilidad no debe impedir ver rutas con información previamente disponible.

## Nivel 1 — Contexto

```mermaid
flowchart LR
  OP[Operador de despacho] -->|planifica y monitorea| SYS[EcoLogística Lima]
  CON[Conductor] -->|consulta ruta y reporta incidencias| SYS
  ANA[Analista / Auditor] -->|consulta indicadores y reportes| SYS
  SYS -->|consulta rutas, mapa y tráfico| MAP[OSM / proveedor de tráfico]
  SYS -->|genera PDF| PDF[Servicio de generación de reportes]
```

## Nivel 2 — Contenedores

```mermaid
flowchart LR
  U[Usuarios] -->|HTTPS| WEB[Aplicación web React]
  WEB -->|REST JSON / HTTPS| API[API FastAPI]
  API -->|SQL + PostGIS| DB[(PostgreSQL)]
  API -->|encola trabajo| Q[(Redis / cola)]
  WORKER[Worker de optimización Python] -->|consume| Q
  WORKER -->|lee y guarda resultados| DB
  API -->|adaptador| EXT[Mapa / tráfico externo]
  API -->|PDF| REP[Generador de reportes]
```

| Contenedor | Tecnología | Responsabilidad |
|---|---|---|
| Aplicación web | React/TypeScript | Experiencia de despacho, administración, modo conductor y accesibilidad. |
| API | FastAPI | Autenticación, RBAC, validación, casos de uso y OpenAPI. |
| Worker | Python | Ejecuta metaheurística, reintentos y persistencia de resultados. |
| Base de datos | PostgreSQL/PostGIS | Datos del dominio, auditoría y geodatos. |
| Cola/caché | Redis | Desacopla cálculos y limita reintentos. |

## Nivel 3 — Componentes de la API

```mermaid
flowchart TD
  CTRL[Controladores REST] --> AUTH[Autenticación y autorización]
  CTRL --> APP[Servicios de aplicación]
  APP --> FLEET[Servicio flota y conductores]
  APP --> ORDER[Servicio pedidos y clientes]
  APP --> PLAN[Servicio planificación]
  APP --> KPI[Servicio indicadores/reportes]
  APP --> ADAPT[Adaptadores mapas/tráfico]
  FLEET --> REPO[Repositorios]
  ORDER --> REPO
  PLAN --> REPO
  KPI --> REPO
  REPO --> DB[(PostgreSQL)]
  PLAN --> QUEUE[Cola de optimización]
  CTRL --> AUDIT[Auditoría]
  AUDIT --> DB
```

## Flujos críticos

```mermaid
sequenceDiagram
  actor O as Operador
  participant W as Web React
  participant A as API FastAPI
  participant Q as Cola Redis
  participant T as Worker de optimización
  participant D as PostgreSQL/PostGIS
  O->>W: Solicita optimización
  W->>A: Envía pedidos y recursos
  A->>D: Valida y crea ejecución
  A->>Q: Encola trabajo
  A-->>W: Devuelve identificador y estado
  Q->>T: Entrega trabajo
  T->>D: Lee datos y parámetros vigentes
  T->>D: Guarda rutas, excepciones y métricas
  W->>A: Consulta estado
  A->>D: Obtiene resultado
  A-->>W: Rutas y métricas
```

1. El Operador envía pedidos y recursos al servicio de planificación.
2. La API valida permisos, datos y reglas; crea una optimización reproducible y la encola.
3. El worker obtiene parámetros vigentes, calcula una solución y persiste rutas, excepciones y métricas.
4. La web consulta el estado, muestra mapa/datos con hora de fuente y permite la decisión final del operador.

## Seguridad y operación

HTTPS termina en el borde de la aplicación; secretos se gestionan fuera del repositorio. La API valida esquemas, aplica mínimo privilegio y registra acciones relevantes. Las métricas incluyen latencia P95, duración de trabajos, errores, disponibilidad y consultas lentas. Las copias de seguridad y la restauración se verifican antes de liberar el MVP.
